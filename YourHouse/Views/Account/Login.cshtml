@model YourHouse.Web.Models.LoginAccount
@{
    ViewData["Title"] = "Login";
}

<div class="container">
    <div class="row">
        <div class="col">
            <h3>Đăng nhập</h3>
            <form class="form-control" action="/Account/Login" method="post">
                <div class="mt-3">
                    <label class="form-label" asp-for="Email">Nhập email</label>
                    <input class="form-control" type="email" asp-for="Email" placeholder="Abc@gmail.com, ..." />
                    <span class="text-danger small" asp-validation-for="Email"></span>
                </div>
                <div class="mt-3">
                    <label class="form-label" asp-for="Password">Nhập mật khẩu</label>
                    <div>
                        <input class="form-control" id="Password" type="password" asp-for="Password" />
                        @* <i class="fa-solid fa-eye"></i> *@
                    </div>
                    <span class="text-danger small" asp-validation-for="Password"></span>
                </div>
                <button class="btn btn-outline-primary mt-3">Đăng nhập</button>
            </form>
        </div>
    </div>

